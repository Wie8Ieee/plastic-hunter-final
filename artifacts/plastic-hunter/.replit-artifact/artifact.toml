kind = "web"
previewPath = "/"
title = "Plastic Hunter AI"
version = "1.0.0"
id = "artifacts/plastic-hunter"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8000

[services.development]
run = "cd /home/runner/workspace && python -m uvicorn main:app --host 0.0.0.0 --port 8000"

[services.env]
PORT = "8000"
